#include <stdio.h>
#include <stdlib.h>
#include "param_list.h"

struct param_list * param_list_create( char *name
				       struct type *type,
				       struct param_list *next ) { return NULL; }
void param_list_print( struct param_list *a ) {}
